#!/usr/bin/env bash
set -euo pipefail

COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.local.yml}"

echo "[down] Stopping local stack using ${COMPOSE_FILE}..."
docker compose -f "${COMPOSE_FILE}" down

echo "[down] Done."
