#!/usr/bin/env bash
set -euo pipefail

COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.local.yml}"

echo "[up] Starting full local stack using ${COMPOSE_FILE}..."
docker compose -f "${COMPOSE_FILE}" up -d --build

echo "[up] Done."
echo "[up] App: http://localhost:8080"
