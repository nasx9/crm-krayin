#!/usr/bin/env bash
set -euo pipefail

COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.local.yml}"

if [[ $# -lt 1 ]]; then
  echo "Usage: bin/npm <npm args...>"
  exit 1
fi

docker compose -f "${COMPOSE_FILE}" exec vite npm "$@"
