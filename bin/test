#!/usr/bin/env bash
set -euo pipefail

COMPOSE_FILE="${COMPOSE_FILE:-docker-compose.local.yml}"
SERVICE="${SERVICE:-krayin}"

echo "[test] Running tests..."
docker compose -f "${COMPOSE_FILE}" exec "${SERVICE}" php artisan test
echo "[test] Done."
